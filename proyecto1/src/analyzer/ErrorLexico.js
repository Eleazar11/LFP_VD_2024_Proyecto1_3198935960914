class ErrorLexico {
    constructor(valor, descripcion) {
        this.valor = valor; // El valor o carácter que causó el error
        this.descripcion = descripcion; // Descripción del error
    }
}
